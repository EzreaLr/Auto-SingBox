name: Sync Upstream Repository

on:
  schedule:
    - cron: '0 16 * * *'  # 每天 UTC 16:00 (北京时间 00:00)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Fetch upstream
        run: |
          git remote add upstream https://github.com/0xdabiaoge/singbox-lite.git 2>/dev/null || true
          git fetch upstream main --depth=1

      - name: Check for updates
        id: check
        run: |
          if git diff --quiet HEAD upstream/main -- singbox.sh README.md; then
            echo "has_updates=false" >> $GITHUB_OUTPUT
            echo "✅ 无更新"
          else
            echo "has_updates=true" >> $GITHUB_OUTPUT
            echo "🔄 发现更新"
          fi

      - name: Sync files
        if: steps.check.outputs.has_updates == 'true'
        run: |
          # 同步 singbox.sh
          git checkout upstream/main -- singbox.sh
          chmod +x singbox.sh
          
          # 同步 README.md 并替换安装命令
          git show upstream/main:README.md | sed 's|curl -LfsS https://raw.githubusercontent.com/0xdabiaoge/singbox-lite/refs/heads/main/singbox.sh.*|curl -LfsS https://raw.githubusercontent.com/Acone0/singbox-lite/refs/heads/main/singbox.sh -o /usr/local/bin/sb \|\| wget -q https://raw.githubusercontent.com/Acone0/singbox-lite/refs/heads/main/singbox.sh -O /usr/local/bin/sb) \&\& chmod +x /usr/local/bin/sb \&\& sb|' > README.md
          
          echo "✅ 文件已同步并保留自定义命令"

      - name: Commit and push
        if: steps.check.outputs.has_updates == 'true'
        run: |
          git add singbox.sh README.md
          git commit -m "🔄 同步上游更新 ($(date -u '+%Y-%m-%d'))" || exit 0
          git push